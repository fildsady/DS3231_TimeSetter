<Window x:Class="DS3231_TimeSetter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DS3231 Time Setter (HID)" Height="380" Width="420" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Connect -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="btnConnect" Content="Connect" Width="100" Height="30" Click="BtnConnect_Click"/>
            <TextBlock x:Name="txtStatus" Text="Disconnected" VerticalAlignment="Center" Margin="12,0" Foreground="Gray"/>
        </StackPanel>

        <!-- PC Time -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="PC Time:" VerticalAlignment="Center" Width="70"/>
            <TextBlock x:Name="txtPcTime" Text="--:--:--" FontSize="20" FontWeight="Bold" Foreground="DarkBlue"/>
        </StackPanel>

        <!-- RTC Time -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="RTC Time:" VerticalAlignment="Center" Width="70"/>
            <TextBlock x:Name="txtRtcTime" Text="--:--:--" FontSize="20" FontWeight="Bold" Foreground="DarkGreen"/>
            <TextBlock x:Name="txtTemp" Text="" FontSize="14" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="Gray"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,8">
            <Button x:Name="btnSyncTime" Content="Sync PC Time → RTC" Width="160" Height="36" FontSize="14"
                    Click="BtnSyncTime_Click" IsEnabled="False"/>
            <Button x:Name="btnReadTime" Content="Read RTC" Width="100" Height="36" FontSize="14" Margin="8,0"
                    Click="BtnReadTime_Click" IsEnabled="False"/>
        </StackPanel>

        <!-- Status -->
        <TextBlock Grid.Row="4"/>

        <!-- Log -->
        <TextBox Grid.Row="5" x:Name="txtLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" Margin="0,8,0,0" FontFamily="Consolas" FontSize="11"/>
    </Grid>
</Window>
